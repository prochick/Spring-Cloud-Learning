server:
  port: 8021
  servlet:
    context-path: /

spring:
  application:
    name: ribbon-service

eureka:
  instance:
    instance-id: ribbon-service1-8021
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8010/eureka

